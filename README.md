# Interactive Cyberpunk HUD Portfolio

**[RU]** Иммерсивное портфолио в стиле Киберпанк для разработчика Unreal Engine. Написано на чистом JS с применением игровых механик. Включает интерактивный HUD, кастомный видеоплеер, аудио-движок и систему мгновенного перевода.

**[EN]** An immersive Cyberpunk-styled portfolio for an Unreal Engine developer. Built with Vanilla JS using GameDev principles. Features a fully interactive Sci-Fi HUD, custom video player, dynamic audio engine, and real-time localization.

---

Это не просто статичная верстка, а полноценное одностраничное приложение (SPA), использующее подходы из GameDev: игровой цикл (Game Loop), процедурную анимацию и кастомный аудио-движок.

## ✨ Ключевые особенности (Key Features)

### 🎮 Визуальный стиль и UX
* **Стилистика Sci-Fi HUD:** Интерфейс имитирует дисплей шлема или терминала. Использованы SVG-маски, clip-path геометрия и процедурные декоративные элементы.
* **Кастомный курсор:** Реализована механика "Lag & Lerp" (линейная интерполяция). Основной прицел движется мгновенно, а внешнее кольцо плавно догоняет его с инерцией. При наведении на активные элементы курсор меняет состояние (Target Lock).
* **Matrix Background:** Фон генерируется процедурно через HTML5 Canvas, отрисовывая падение символов (цифровой дождь) без нагрузки на DOM.
* **3D Tilt Effect:** Карточки проектов имеют физически корректный наклон, реагирующий на положение мыши в реальном времени.

### ⚙️ Инженерные решения
* **Optimization First:** Все тяжелые анимации (курсор, параллакс, тильт) работают внутри `requestAnimationFrame` для обеспечения стабильных 60 FPS. Избегаются тяжелые CSS-транзишны на свойствах top/left, используется только аппаратное ускорение через transform.
* **Smooth Scroll:** Интегрирована библиотека `Lenis` для плавной инерционной прокрутки, создающей ощущение "премиального" интерфейса.
* **Адаптивность:** Полная поддержка мобильных устройств. На тач-скринах отключаются курсорные эффекты, меняется логика слайдеров (Scroll Snap), а сложные ховеры заменяются на статические состояния.

### 🎥 Медиа-система
* **Custom Video Player:** Полностью переписанный интерфейс видеоплеера. Стандартные контролы браузера скрыты. Реализованы собственные бары прогресса и громкости, поддержка горячих клавиш (Space, стрелки), смена скорости воспроизведения и блокировка ориентации экрана на мобильных устройствах.
* **Audio Engine:**
    * Фоновая музыка с плавным фейдом.
    * Система звуковых эффектов (SFX) при наведении и кликах (Hover/Click UI sounds).
    * Глобальный переключатель звука и отдельный тумблер для SFX.
    * *Mobile Fix:* Реализована логика "разблокировки" AudioContext при первом касании экрана для обхода ограничений мобильных браузеров.

### 🌐 Локализация (Localization)
* **Real-time Translation:** Встроенная система перевода (RU/EN). Тексты хранятся в дата-атрибутах (`data-ru`, `data-en`) и подменяются "на лету" без перезагрузки страницы.
* **Hacker Text Effect:** При смене языка или наведении на заголовки происходит анимация расшифровки символов (Decipher effect).

## 🛠 Используемые технологии

Проект написан на нативном стеке без использования тяжелых фреймворков (React/Vue), чтобы сохранить максимальный контроль над рендерингом и производительностью.

* **Core:** HTML5, CSS3 (CSS Variables, Flexbox/Grid), JavaScript (ES6+).
* **Libraries:**
    * `Lenis` — плавный скролл.
    * `RemixIcon` — векторные иконки.

## 🚀 Установка и запуск

1. Откройте `index.html` в любом современном браузере.
2. Для корректной работы путей к ассетам (видео/звук) рекомендуется использовать локальный сервер (например, расширение **Live Server** для VS Code).

## 📝 Настройка контента

Весь контент редактируется напрямую в `index.html`.

* **Тексты:** Меняйте содержимое тегов и атрибуты `data-ru`/`data-en` для перевода.
* **Ссылки:** Обновите `href` в секции контактов и футере.
* **Проекты:** В секции `#projects` можно дублировать блоки `.project-card` для добавления новых работ.
* **Медиа:** Замените файлы в папке `assets/` на свои, сохранив имена файлов, или обновите пути в коде.

## 👨‍💻 Автор

**Tsuyu™**

*P.S. Текст документации сгенерирован нейросетью, потому что разработчику было откровенно лень писать это самому (да здравствует автоматизация).*
